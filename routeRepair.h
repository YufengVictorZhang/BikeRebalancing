#pragma once


#include <string>
#include <vector>
#include <iostream>

using namespace std;


void connectRoutes() {

	//for (auto iter = routes.begin(); iter != routes.end(); iter++) {
	//	vector<string> R = *iter;
	//	int time = 0;
	//	for (auto iter = R.begin(); iter != R.end; iter++) {

	//	}
	//}

	
	return;
}